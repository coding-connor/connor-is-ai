apiVersion: v1
kind: Service
metadata:
  name: {{.Values.frontend.name}}
spec:
  type: ClusterIP
  ports:
    - port: {{.Values.frontend.service.port}}
      targetPort: 3000
      protocol: TCP
  selector:
    app: {{.Values.frontend.name}}
